# iOS mobile build

The QML mobile UI can be built with Qt for iOS.

Planned native bridge:
- AVSpeechSynthesizer for system TTS
- app sandbox storage for phrases/settings
- online Google/Edge engines later where allowed

iOS cannot freely install Python or external binaries, so Piper/edge-tts parity requires a different native approach.
